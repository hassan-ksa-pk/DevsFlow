// Compatibility layer for BotForge-sourced components that expect `@/contexts/AuthContext`.
// DevsFlow's auth context lives in `@/hooks/useAuth`.
export { AuthProvider, useAuth } from "@/hooks/useAuth";

